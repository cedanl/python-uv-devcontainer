{
  "name": "python-uv-dev",
  "build": { "dockerfile": "Dockerfile" },
  "extensions": [
    "ms-python.python",
    "ms-python.vscode-pylance",
    "anthropic.claude-code"
  ],
  "postCreateCommand": "uv sync && npx skills add cedanl/.github --skill '*' -a claude-code -a opencode -y --copy -g",
  "forwardPorts": [8501],
  "remoteUser": "vscode"
}
